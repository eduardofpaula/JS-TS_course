const nome1 = 'edu';
const nome2 = "edu";
const nome3 = `edu`;
const num1 = 10; //number
const num2 = 20.53; //number
let nomeAluno; //undefined -> não aponta pra local nenhum na memória
const sobreNomeAluno = null; //Nulo -> não aponta para local nenhum na memoria
const aprovado = false; //Boolean = true,false (logico)